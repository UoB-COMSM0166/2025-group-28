class Game {
  // Arrays to keep track of active game assets

  constructor(lvl, player) {
    this.level = lvl;
    this.currentRoom = 0;
    this.gameState = 0;
    this.player = player;
  }

  handlleInput(key) {}
}
