/* GLOBAL CONSTANTS */
let game;

function preload() {
  // preload images in to global vars here
}

function setup() {
  createCanvas(750, 750);

  game = new Game();
}

function draw() {
  background(10, 11, 28);
}
